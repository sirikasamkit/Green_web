{
  "name": "green-web-analyzer",
  "version": "1.0.0",
  "description": "Unified Full-Stack Green Web Carbon Analyzer",
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server --omit=dev",
    "start": "node server/src/app.js",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm start --prefix server"
  }
}
