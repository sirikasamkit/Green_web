# Server Port
PORT=5000

# Environment Mode (development | production)
NODE_ENV=development

# SQLite Database File Path
DB_PATH=./data/database.sqlite

# Optional: Redis Cache URL (if using Docker/Redis)
# REDIS_URL=redis://localhost:6379
